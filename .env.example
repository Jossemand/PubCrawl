# Copy this file to `.env` and fill in the values from your Supabase project
# (Supabase dashboard → Project Settings → API).
#
# Leave BOTH blank (or delete .env) to run the app on localStorage only —
# the app works fully offline with no backend when these are unset.

PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
