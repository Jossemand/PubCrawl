// Pure client-side SPA: no server rendering (state lives in the browser), and a
// fallback shell is emitted so any static host can serve every route.
export const ssr = false;
export const prerender = false;
